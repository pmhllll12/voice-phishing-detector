# =============================================================================
# namespace.yaml
#
# 전체 스택(api/mcp-server/rag-worker/stt-worker/frontend/postgres/prometheus/
# grafana)을 격리하는 전용 네임스페이스. kustomization.yaml의 최상위 `namespace:`
# 필드가 이 네임스페이스 안의 모든 리소스에 자동으로 붙는다.
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: vps-detector
