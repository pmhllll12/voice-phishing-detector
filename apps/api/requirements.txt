# apps/api 최소 의존성
# TODO: 버전 핀 고정 (pip freeze 후 반영)
fastapi
uvicorn[standard]
prometheus-client
pydantic
httpx
python-multipart  # F-03: /api/v1/calls/deepvoice-check 오디오 업로드(UploadFile)에 필요
numpy  # F-03: 음향 특징(피치/스펙트럼) 계산 (infrastructure/adapters/deepvoice_adapter.py)
psycopg[binary]  # N-01: 감사증적(postgres) — infrastructure/adapters/postgres_call_log_repository.py

# F-03 v2: 검증된 오픈소스 스푸핑 탐지 모델(wav2vec2 기반, HuggingFace Hub) —
# infrastructure/adapters/wav2vec2_deepvoice_adapter.py 상단 주석 참고. CPU로 충분해
# CUDA 인덱스 없이 기본 PyPI에서 설치한다(rag-worker/torch의 GPU 인덱스 지정과 다른 이유는
# 해당 파일 "WHY CPU" 주석 참고).
torch --index-url https://download.pytorch.org/whl/cpu
transformers
