# apps/rag-worker 최소 의존성
fastapi
uvicorn[standard]
prometheus-client
httpx

# F-04 v1은 외부 임베딩 없이 문자 bigram TF-IDF(순수 Python, stdlib만 사용)로 구현됨
# (infrastructure/adapters/tfidf_similarity_adapter.py 참고) — 추가 의존성 없음.
#
# TODO: 검색 품질이 부족하면 아래로 교체 검토
#   - 로컬 오픈소스: sentence-transformers
#   - 외부 API: anthropic / openai 등 embedding endpoint 사용 클라이언트
# TODO: pgvector 연동 (asyncpg 또는 sqlalchemy + pgvector 확장)
