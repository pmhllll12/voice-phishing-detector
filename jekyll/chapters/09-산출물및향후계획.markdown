---
layout: page
title: 산출물 및 향후 계획
permalink: /09-산출물및향후계획/
---

## 1) 산출물 계획

| 구분 | 산출물 | 상태 |
|---|---|---|
| 문서 | `docs/RFP.md` (제안요청서) | 완료 |
| 문서 | `docs/design.md` (설계서) | N-06(확장성) 챕터만 완료, 나머지 챕터는 TODO |
| 문서 | `docs/requirements.md` (요구사항정의서) | TODO |
| 문서 | `docs/test-plan.md` (시험계획서) | TODO |
| 코드 | `apps/api` — 탐지 오케스트레이션 | F-01/F-02/F-05, N-01~N-05 반영 완료 |
| 코드 | `apps/mcp-server` — 도구 분리(MCP+REST) | F-01, F-04, F-07, N-02 반영 완료 |
| 코드 | `apps/rag-worker` — 유사사례 검색 | F-04 완료(pgvector 전환까지) |
| 코드 | `apps/stt-worker` — 모바일 오디오 STT | F-05 오디오 경로 연동 완료 |
| 코드 | `apps/frontend` — 관제 대시보드 | F-06 완료 |
| 인프라 | `docker-compose.yaml`, `infra/db/init.sql`, `prometheus/prometheus.yml` | 스키마/스킬 완료, compose 실기동·EC2 배포는 TODO |
| 도구 | `.mcp.json` (Claude Code MCP 등록), `run-voice-phishing-detector` 스킬 | 완료 |
| 이 문서 | Jekyll 제안서 사이트(`jekyll/`) — 지금 보고 있는 이 문서 | 완료(2026-08-31 기준 F-01~F-07/N-01~N-06 반영) |

## 2) 기대효과 및 활용방안

- **포트폴리오로서의 활용** — AI 데이터센터/AI 인프라 엔지니어 직무 지원 시, "AI
  판정 파이프라인을 만든다"에 그치지 않고 관측성(Prometheus 커스텀 메트릭), 접근통제
  (RBAC), 감사증적(append-only), 헥사고날 아키텍처를 통한 확장성까지 인프라 엔지니어
  관점의 요구사항을 함께 다뤘다는 근거로 제시할 수 있다.
- **재현 가능한 실증 자료** — "포트-어댑터로 재설계 없이 확장 가능하다"는 주장을
  실제 커밋(3개 축 교체, application 계층 diff 0줄)으로 증명한 것처럼, 이 저장소의
  커밋 히스토리 자체가 "설계 원칙을 실제로 지켰는가"를 검증할 수 있는 자료가 된다.
- **다음 프로젝트로의 재사용** — 이번에 검증한 인프라 스킬(Docker Compose, Prometheus/
  Grafana, 헥사고날 아키텍처, EC2/Cloudflare 배포)은 이미 이전 프로젝트
  (gpu-fleet-ops)에서 왔고, 이번 프로젝트에서 다시 검증됐으므로 다음 포트폴리오
  프로젝트에도 동일하게 이식 가능하다.

## 3) 향후 개선 과제

우선순위가 높은 순서대로 정리한다(8장 3절 위험요소와 겹치는 항목은 대응 방향을
그대로 가져왔다).

1. ~~F-03 딥보이스 v2 교체~~ 완료(2026-08-31) — `DeepvoiceDetectionPort` 인터페이스를
   그대로 유지한 채(N-06, 0줄 변경) wav2vec2 기반 스푸핑 탐지 모델로 교체했다.
   다음 과제는 이 모델의 일반화 여부를 더 큰 데이터셋으로 검증하는 것이다(8장 3절).
2. **`docker-compose.yaml` 실기동 검증 + EC2 배포** — 현재는 뼈대만 있고 TODO로
   표시된 항목(healthcheck, .env 분리, 네트워크 정책 등)이 남아있다.
3. **`docs/requirements.md`/`docs/test-plan.md` 작성** — RFP를 요구사항정의서·
   시험계획서 수준으로 구체화.
4. **N-05 SLA 실트래픽 관측** — 지금은 메트릭 배선만 완료했으므로, Grafana에서
   p95/p99 응답시간 대시보드를 구성하고 실제 부하 시나리오로 SLA 충족 여부를
   확인한다.
5. **postgres 단일 장애점 완화** — `/ready`가 다운을 반영하는 것과 별개로, 재기동
   자동화(systemd `Restart=`, docker healthcheck) 또는 복제/백업 정책을 추가한다.
6. **PII 마스킹(N-03) 정량 평가** — 특히 이름 마스킹의 오탐/누락률을 정량 데이터셋으로
   측정한다.
7. **mcp-server 신규 진입점 확장 검증** — REST/MCP stdio 외 세 번째 진입점(예: gRPC)을
   추가해봄으로써 N-06의 확장성 주장을 진입점 배선 축에서도 실측으로 검증한다.

[← 목차로]({{ "/toc/" | relative_url }})
